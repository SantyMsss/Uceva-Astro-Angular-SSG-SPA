import { Component } from '@angular/core';

/**
 * Página de contacto.
 *
 * @remarks
 * Contiene un formulario de contacto con campos
 * de nombre, email y mensaje (solo UI, sin backend).
 */
@Component({
  selector: 'app-contact-page',
  templateUrl: './contact.page.html',
  standalone: true
})
export class ContactPage {}
